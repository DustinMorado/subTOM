=============
extract_noise
=============

Finds and extracts noise volumes from a tomogram to generate an average
amplitude spectrum of the noise, which can be used as a Fourier weight in place
of a traditional binary-wedge. Also generates an estimated binary wedge as well
from the noise.

